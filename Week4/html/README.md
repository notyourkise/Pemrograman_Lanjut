# Week 4 — CSS Lanjutan + JS: Toggle Tema (Dark/Light)

Fokus Minggu 4:
- Memperkuat CSS dengan custom properties (variabel), hover/focus/active, transition, card hover, responsive kecil.
- Menambahkan tombol toggle tema dark/light menggunakan JavaScript sederhana.

Tujuan Pembelajaran:
- Mengorganisasi tema menggunakan variabel CSS (`--bg`, `--text`, `--primary`).
- Mengubah tema dengan menambah/menghapus kelas `dark` pada `<html>` atau `<body>`.
- Menyimpan preferensi pengguna (opsional) dengan `localStorage`.

Struktur:
- `index.html`, `about.html`, `projects.html`, `project-detail.html`, `contact.html` — halaman portofolio.
- `assets/css/styles.css` — stylesheet dengan dukungan tema.
- `assets/js/theme-toggle.js` — script untuk mengatur toggle tema.

Tugas Mingguan:
1) Tambahkan tombol toggle tema di header (ikon 🌙/☀️) yang memanggil fungsi JS.
2) Implementasikan variabel CSS untuk warna utama dan latar; buat set warna untuk light & dark.
3) Pastikan transisi halus saat pergantian tema.
4) (Opsional) Simpan preferensi ke `localStorage` dan muat saat halaman dibuka.

Checklist:
- [ ] Toggle mengubah tema di semua halaman.
- [ ] Kontras warna memadai (teks tetap terbaca).
- [ ] Tidak ada error di konsol.
- [ ] Lulus validasi HTML & CSS.
